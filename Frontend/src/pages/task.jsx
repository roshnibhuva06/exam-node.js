import React from "react";
export default function Task() {
    return (
        <div>
            <h2>Task Page</h2>
            <p>This is the task page. Only authenticated users can see this.</p>
        </div>
    );
}